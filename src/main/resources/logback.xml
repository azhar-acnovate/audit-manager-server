<configuration>

	<property name="LOG_FILE" value="audit-mananger" />
	<property name="LOG_DIR" value="${user.home}" />

	<appender name="FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		    
		<file>${LOG_DIR}/${LOG_FILE}.log</file>
		    
		<rollingPolicy
			class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			        
			<fileNamePattern>${LOG_DIR}/%d{yyyy/MM/dd}/${LOG_FILE}.%i.gz
			</fileNamePattern>
			       <!--  <totalSizeCap>3GB</totalSizeCap> -->
			<timeBasedFileNamingAndTriggeringPolicy
				class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<maxFileSize>50MB</maxFileSize>
			</timeBasedFileNamingAndTriggeringPolicy>
			    
		</rollingPolicy>
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
			</pattern>
		</encoder>
	</appender>


	<appender name="STDOUT"
		class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
			</pattern>
		</encoder>
	</appender>


	<logger name="org.hibernate" level="ERROR" />

	<logger name="org.springframework" level="ERROR" />
	<logger name="org.springframework.transaction" level="ERROR" />
	<logger name="org.springframework.security" level="ERROR" /> <!-- to debug security related issues (DEBUG) -->
	<logger name="org.springframework.web.servlet.mvc" level="ERROR" /> <!-- some serialization issues are at trace level here: org.springframework.web.servlet.mvc.method.annotation.ServletInvocableHandlerMethod -->

	<!-- our service -->
	<logger name="com.acnovate.audit_manager" level="INFO" />

	<root level="INFO">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="FILE" />
	</root>

</configuration>